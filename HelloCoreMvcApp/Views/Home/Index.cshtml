@using HelloCoreMvcApp.Models.Products
@model IEnumerable<HelloCoreMvcApp.Models.Products.Phone>
@{
    ViewData["Title"] = "Buy whatever you want and get nothing";
    var product = ViewData["ProductDay"] as Phone;
    Random rnd = new Random();
}

<div class="jumbotron">
    <div class="row">
        <div class="col-sm-4">
            <img class="img-fluid" src="@("https://source.unsplash.com/800x800/?phone-" + product.Company )" />
        </div>
        <div class="col-sm-8" style="display:flex; flex-direction: column;">
            <h1 class="display-custom">PRODUCT OF THE DAY</h1>
            <h4 class="font-weight-bold"> @product.Company @product.Name &nbsp;&nbsp;&nbsp;&nbsp; $@product.Price</h4>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Quis ipsum suspendisse ultrices gravida. Risus commodo viverra maecenas accumsan lacus vel facilisis. </p>
            <div style="margin-top: auto;">
                <button class="btn btn-primary">BUY</button> <button class="btn btn-secondary">ADD TO SHOPPING CART</button>
            </div>

        </div>
    </div>
</div>

<div class="border-bottom category-name"><a href="">// SMARTPHONES</a></div>
<div class="container category">
    <div class="row card-deck">
        @for (int i = 0; i < 8; i++)
        {
            int id = rnd.Next(0, Model.Count());
            var phone = Model.ElementAt(id);
            <div class="col-3">
                <a href="@("/catalog/phones/" + phone.Id)">
                    <div class="card">
                        <div class="card-body">
                            <u class="card-title">@phone.Company</u> <u class="card-title float-right">$@phone.Price</u> <br />
                            <u class="card-title">@phone.Name</u>
                        </div>
                        <img class="card-img-bottom" src="@( "https://source.unsplash.com/800x800/?" + phone.Company + "-phone-" + phone.Id )">
                    </div>
                </a>
            </div>
        }
    </div>
</div>

<!--

<div class="border-bottom category-name"><a href="">// SMARTPHONES</a></div>
<div class="container category">
    <div class="row card-deck">
        @foreach (var phone in Model)
        {
            <div class="col-3">
                <a href="@("/catalog/phones/" + phone.Id)">
                    <div class="card">
                        <div class="card-body">
                            <u class="card-title">@phone.Company</u> <u class="card-title float-right">$@phone.Price</u> <br />
                            <u class="card-title">@phone.Name</u>
                        </div>
                        <img class="card-img-bottom" src="@( "https://source.unsplash.com/800x800/?" + phone.Company + "-phone-" + phone.Id )">
                    </div>
                </a>
            </div>
        }
    </div>
</div>
-->